const { readdirSync, readFileSync, writeFileSync } = require('fs'); const path = require('path'); function processDir(dir) { const files = readdirSync(dir, { withFileTypes: true }); for (const f of files) { const fullPath = path.join(dir, f.name); if (f.isDirectory()) { processDir(fullPath); } else if (f.name.endsWith('.astro') || f.name.endsWith('.css')) { let c = readFileSync(fullPath, 'utf8'); let changed = false; if (c.includes('padding: 0 1.5rem;')) { c = c.replace(/padding: 0 1\.5rem;/g, 'padding: 0 2.5rem;'); changed = true; } if (c.includes('padding: 5rem 1.5rem 3rem;')) { c = c.replace(/padding: 5rem 1\.5rem 3rem;/g, 'padding: 5rem 2.5rem 3rem;'); changed = true; } if (c.includes('padding: 3rem 1.5rem 3.5rem;')) { c = c.replace(/padding: 3rem 1\.5rem 3\.5rem;/g, 'padding: 3rem 2.5rem 3.5rem;'); changed = true; } if (c.includes('padding: 3rem 1.5rem 4rem;')) { c = c.replace(/padding: 3rem 1\.5rem 4rem;/g, 'padding: 3rem 2.5rem 4rem;'); changed = true; } if (c.includes('padding: 5rem 1.5rem 4rem;')) { c = c.replace(/padding: 5rem 1\.5rem 4rem;/g, 'padding: 5rem 2.5rem 4rem;'); changed = true; } if (changed) { writeFileSync(fullPath, c); console.log('Updated ' + fullPath); } } } } processDir('./src');
